int main(){
decl
    int prog,temp;
    str out;
enddecl
begin
    out = "Enter prog name:";
    temp = exposcall("Write", -2, out);
    temp = exposcall("Read", -1, prog);
    
    temp = exposcall("Exec", prog);

    return 0;
end
}

